---
title: Consumables
description: Set refill alerts for reagent, dosing containers, reservoirs and media.
section: Cora Mobile
order: 12
---

Equipment that consumes something — reagent, dosing liquid, top-off water, filter media — can tell Cora how much is left. Cora can warn you before it runs out.

## Setting a refill alert

Open the device from the **Devices** tab and set the alert threshold on its own page. Typical examples:

| Device | Threshold on |
|---|---|
| Trident | Remaining tests, and waste-water level |
| DŌS head | Remaining container volume |
| ReefBeat dosing unit | Remaining container volume |
| ATO | Reservoir level |
| Mat roller | Remaining days of material |

A consumable alert behaves like any other alert: it appears in the [Alert Center](/help/mobile-alerts), can push to your phone, and clears when the level returns above the threshold.

## Choosing a threshold

Set it far enough ahead that you can act. A threshold that fires the day something runs out gives no warning.

:::warning Some equipment does not stop when empty
A dosing head with an empty container continues running its schedule and reports doses it did not deliver. The refill alert is what prevents this, so set one for every head you dose from.
:::

## After refilling

Reset or update the level on the device's page so the alert clears and the next warning is calculated correctly.
